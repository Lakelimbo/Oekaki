/**
 * Set of types that are used on some spare places,
 * in which is easier to just create a type rather than
 * make a big mess with other functions, classes, libs, etc.
 */

export type CookieSameSite = true | false | 'lax' | 'strict' | 'none' | undefined;
